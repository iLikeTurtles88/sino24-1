// La fonctionnalité IA a été supprimée du portfolio de Sino.
export const initializeChat = () => {
  return null;
};

export const sendMessageToArtist = async (message: string): Promise<string> => {
  return "";
};